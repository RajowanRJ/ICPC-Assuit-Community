// Starting in the name of Almighty Allah!

#include <iostream>
#include <cmath>
using namespace std;

int main () 
{
    long long a,b,c,d;
    cin >> a >> b >> c >> d;
    
    cout << (b * log(a) > d * log(c) ? "YES" : "NO");

}